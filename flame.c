#include <stdio.h>
#include <stdlib.h>
#include "bright.h"

int main(int argc, char **argv) {
    if (argc < 2) {
        printf("Usage: %s [+-]AMOUNT\n", argv[0]);
        return 1;
    }

    // TODO

    return 0;
}
